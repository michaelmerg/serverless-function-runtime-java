subprojects {
  apply plugin: 'java'

  repositories {
    jcenter()
  }

  task copyToLib(type: Copy) {
    into "$buildDir/libs"
    from configurations.runtime
  }

  build.dependsOn(copyToLib)
}

project(':function') {
  dependencies {
    compile project(':runtime')
  }
}
